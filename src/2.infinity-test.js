import { describe, it } from 'node:test'

describe('infinity function', () => {
  it.todo('should increment by 1 for each call, starting from 1, over 10 calls')

  it.todo('should start from 0 by default when the start parameter is not provided')

  it.todo('should decrement by 1 for each call, starting from -1, to -10 when a decrement function is provided')
})

describe('limit function', () => {
  it.todo('should limit n elements from an iterable object')

  it.todo('should throw a TypeError if n is less than 0')
})

describe('odds function', () => {
  it.todo('should only yield odd values when called')

  it.todo('should throw a TypeError if n is less than or equal to 0')
})
